import { ReactiveController, ReactiveControllerHost } from "lit";
export declare class PersistStateController<T = ReactiveControllerHost> implements ReactiveController {
    host: ReactiveControllerHost;
    prefixKey: string;
    properties: Array<keyof T>;
    constructor(host: ReactiveControllerHost, prefixKey?: string, properties?: Array<keyof T>);
    observe(properties: Array<keyof T>, prefixKey?: string): void;
    hostUpdate(): void;
}
//# sourceMappingURL=persist-state-controller.d.ts.map