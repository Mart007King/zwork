import { ReactiveController, ReactiveControllerHost } from "lit";
export type ResizeCondition = {
    target: HTMLElement;
    name: string;
    condition: (entry: ResizeObserverEntry) => boolean;
};
export declare class ResizeController implements ReactiveController {
    constructor(host: ReactiveControllerHost);
    host: ReactiveControllerHost;
    observers: ResizeCondition[];
    conditions: {
        [key: string]: boolean;
    };
    resizer: ResizeObserver;
    observe(resizeCondition: ResizeCondition): void;
    hostDisconnected(): void;
}
//# sourceMappingURL=resize-controller.d.ts.map